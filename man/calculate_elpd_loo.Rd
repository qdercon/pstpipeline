% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_funcs.R
\name{calculate_elpd_loo}
\alias{calculate_elpd_loo}
\title{Calculate the expected log pointwise predictive density via PSIS-LOO}
\usage{
calculate_elpd_loo(fit, cores = getOption("mc.cores", 4))
}
\arguments{
\item{fit}{A \link[cmdstanr:CmdStanMCMC]{cmdstanr::CmdStanMCMC} or \link[cmdstanr:CmdStanVB]{cmdstanr::CmdStanVB} object, e.g.
\code{fit_learning_model(..., outputs = "model_env")$fit}.}

\item{cores}{Number of cores to use for computing PSIS-LOO. Defaults to
\code{options(mc.cores)} or 4 if this is not set.}
}
\value{
A \code{\link[loo:loo]{loo::loo()}} object (class \code{psis_loo} for MCMC fits, or
\code{psis_loo_ap} for variational fits).
}
\description{
\code{calculate_elpd_loo} computes an approximate leave-one-out
cross-validation estimate (\code{\link[loo:loo]{loo::loo()}}) from a fitted
\link[cmdstanr:CmdStanMCMC]{cmdstanr::CmdStanMCMC} or \link[cmdstanr:CmdStanVB]{cmdstanr::CmdStanVB} model's \code{log_lik}
generated quantity. When \code{fit} comes from variational inference,
\code{\link[loo:loo_approximate_posterior]{loo::loo_approximate_posterior()}} is used instead of \code{\link[loo:loo]{loo::loo()}}, since
ordinary PSIS-LOO assumes exact posterior draws.
}
\examples{
\dontrun{
data(example_data)
fit <- fit_learning_model(
  example_data$nd,
  model = "2a",
  exp_part = "training",
  outputs = "model_env"
)
calculate_elpd_loo(fit$fit)
}

}
